syntax = "proto3";

message SpeechRequest {
  bytes pcm = 1;
  string language = 2;
}

message SpeechResponse {
  string text = 1;
  string detected_language = 2;
}

service BuiltinSpeechService {
  rpc RunBuiltinSpeechRecognition(SpeechRequest) returns (SpeechResponse);
}
