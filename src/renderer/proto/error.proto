syntax = "proto3";

/** Machine-readable error code and human-readable message for IPC error responses. */
message Error {
  string code = 1;
  string message = 2;
}

/** Generic single-string response used across multiple services. */
message StringResponse {
  string value = 1;
}
