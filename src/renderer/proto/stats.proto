syntax = "proto3";

import "settings.proto";

/**
 * Request for loading dashboard metrics.
 */
message GetStatsRequest {}

/**
 * Dashboard statistics retrieval contract for the settings dialog.
 */
service StatsService {
  /**
   * Returns current dashboard metrics.
   */
  rpc GetStats(GetStatsRequest) returns (DashboardStats);
}
